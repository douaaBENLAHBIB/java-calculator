# Java Calculator

A simple calculator built in Java with a Swing GUI. Supports basic arithmetic operations.

## How to run
Compile and run `Main.java` using any Java IDE (like IntelliJ) or via command line with `javac` and `java`.
